import { Environment } from "../model/Environment";

export const environment: Environment = {
  production: true,
  appName: 'ESTOQUE-WEB',
  envName: 'PROD',
  test: false,
  BASE_URL: 'https://backend.estoque.com.br:8000'
}
